import React from "react";

const electron = window.require("electron");
const ipcRenderer = electron.ipcRenderer;

const Rename = () => {
  return <div>Rename Things ~</div>;
};

export default Rename;
